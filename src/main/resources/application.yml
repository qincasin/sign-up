server:
  port: 8080
  maxHttpHeaderSize: 65536
  maxPostSize: 4194304

mybatis:
  configuration:
    mapUnderscoreToCamelCase: true
spring:
  datasource:
    url : jdbc:mysql://47.100.253.15:3306/nj_tech?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username : root
    password : Alldo807!
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    filters: stat
    maxActive: 300
    initialSize: 50
    maxWait: 60000
    minIdle: 10
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: select 'x'
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    maxOpenPreparedStatements: 20
